# Ops-console theme — dark + teal (distinct from the credit-debate app).
[theme]
base = "dark"
primaryColor = "#2dd4bf"
backgroundColor = "#0d1117"
secondaryBackgroundColor = "#161b22"
textColor = "#e6edf3"
font = "monospace"
